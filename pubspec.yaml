name: dio_firebase_performance
description: A new Flutter package project.
version: 0.3.0
homepage: https://github.com/eyeem/dio_firebase_performance

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ">=5.0.0 <6.0.0"
  firebase_performance: ">=0.9.0 <10.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
